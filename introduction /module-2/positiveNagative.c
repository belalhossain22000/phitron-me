#include <stdio.h>

int main()
{

    int a;

    scanf("%d", &a);

    if (a  > 0)
    {
        printf("the number is positive =%d", a);
    }
    else
    {
        printf("the number is negative %d", a);
    }

    return 0;
}